<%= form_for(@group_entry) do |f| %>
  <% if @group_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_entry.errors.count, "error") %> prohibited this group_entry from being saved:</h2>

      <ul>
      <% @group_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word_id %><br />
    <%= f.text_field :word_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
